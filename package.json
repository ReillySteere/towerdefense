{
  "name": "rise-of-the-machines",
  "private": true,
  "workspaces": [
    "ui",
    "server",
    "shared"
  ],
  "scripts": {
    "start": "concurrently \"npm run start --workspace=ui\" \"npm run start:dev --workspace=server\"",
    "test": "npm run test --workspace=ui && npm run test --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1"
  }
}
